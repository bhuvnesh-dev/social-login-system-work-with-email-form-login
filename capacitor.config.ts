import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Social login system work with Email form login',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
